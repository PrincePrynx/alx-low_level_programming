#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - starting point
 * Description: prints numbers
 * Return: set at 0 (success)
 */

int main(void)
{
int i;
for (i = 0; i < 10; i++)
	printf("%i", i);

printf("\n");
return (0);
}
